# focus time

Focus time is an application in python to help you focus for a set amount of time

## Installation

- Download the repo
- In the terminal (Anaconda prompt used for testing), navigate to the folder containing the repo
```powershell
pip install .
```


## Usage

To start a focus session, simply write the command
```powershell
focus
```
